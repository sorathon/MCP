{
  "name": "myqaproject",
  "version": "1.0.0",
  "description": "Playwright E2E Test Project — Document Approval System",
  "scripts": {
    "test":        "playwright test --config=playwright.config.ts",
    "test:headed": "playwright test --config=playwright.config.ts --headed",
    "test:debug":  "playwright test --config=playwright.config.ts --debug",
    "test:report": "playwright show-report reports/html-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "typescript": "^5.0.0"
  }
}
